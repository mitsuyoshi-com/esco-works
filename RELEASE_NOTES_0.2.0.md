# ESCO Works v0.2.0 — 更新内容

## 追加した機能

1. プロジェクトを作成・編集し、配下のセッションをツリーで展開／折りたたみ。
2. プロジェクトごとに標準の作業フォルダ・共通指示を設定。
3. セッションの名前変更、所属プロジェクト変更、ピン留め。
4. アーカイブ、ゴミ箱、復元。完全削除にはアプリ内の確認あり。
5. 同一ウィンドウで複数セッションを切り替えて並行作業。作業中・確認待ち・完了・未読を表示。
6. 同じフォルダまたは親子フォルダの作業は順番待ち。待機中の停止にも対応。
7. 左にプロジェクト／履歴、中央に会話、右に作業フォルダの3列構成。
8. ファイル一覧の自動更新、テキスト系ファイルのプレビュー。その他の形式は既定アプリで開く。
9. 常設の「私の業務を教える」。自由な入力をAIが業務別に整理。
10. 通常セッションの本人の入力を「業務共有に追加」から取り込む。
11. スタッフ別の入力記録・業務一覧・業務別MDを共有フォルダに保存。
12. 本人の説明／未確認事項／AI改善案を分離。既存業務ファイルを更新。
13. 自動更新領域の外への手動追記を保持。変更前のMDを `_更新履歴` に保存。
14. 回線切断・共有先未設定でも入力をPCへ保存。定期的に共有先への保存を再試行。
15. 自動領域が他のPCや手動で変更されていた場合は上書きを止め、確認が必要なことを表示。
16. 会話・プロジェクト・共有待ちデータを再起動後も保持。既存の保存済み会話履歴も読み込み可能。
17. お名前・共有先が未設定の場合は起動時に設定画面を表示し、業務入力の始め方を案内。

## 初回設定

1. パソコン版Google Driveで、共有先にアクセスできる状態にします。ストリーミング運用を想定しています。
2. ESCO Worksの設定で本人の「お名前」を登録します。
3. 「業務MDの共有保存 → 共有フォルダを選択」で、Google Drive内の `ESCO_業務共有` を選びます。
4. 「私の業務を教える」に仕事内容を入力して送信します。

スタッフの保存フォルダはPC側で生成した固有IDで分離します。MDの中に担当者名を記載します。PC交換時にはアプリの業務データとスタッフIDを引き継ぐ必要があります。

## 保存・運用上の範囲

- セッション履歴の削除は、作成したExcel・PDFや共有済みMDを削除しません。
- 会話履歴はPC内、業務共有は設定した共有先へ保存します。全会話を自動公開する設定ではありません。
- 共有先へのファイル保存後、クラウドへの同期はパソコン版Google Driveが行います。
- AIが整理形式を返さなかった場合も入力記録は保持し、整理待ちを表示します。
- 過去の会話の一括インポート、スマホ版のプロジェクト画面、個人専属AIの生成、管理者向け自動分析画面は今回の実装には含みません。
- プレビューは512KB以下のテキスト系ファイルに対応します。PDFやExcelの画面内プレビューは未対応です。

## 検証

- 保存・更新テスト：`node test/sessions-test.cjs` / `node test/workspace-test.cjs`
- Electron画面・IPCテスト：`electron test/workspace-ui.cjs`
- 別プロセス再起動：`electron test/workspace-ui.cjs --restore <TEST_DATA>`
- スマホ連携回帰：remote-sim 19項目 / relay-sim 27項目
- 配布ファイル診断：`ESCO Works.exe --workspace-check`（独立した一時保存先。API呼び出しなし）

配布版を実際の社内APIキーで起動し、架空の請求書業務を2回入力するテストに成功しました。AI応答・会話の継続・構造化した業務整理・共有ドライブ上のMD上書き保存を確認しています。各スタッフPCへの更新適用とGoogle Driveのクラウド同期は、各PCでの確認が必要です。
