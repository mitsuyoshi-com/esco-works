## v0.3.0：登録不要のスマホ・PC同期

PC上部の「スマホ・PC同期」から初回だけ名前を入力し、管理者がPCを承認します。スマホはQRを読み、PCで確認番号を照合して許可すると接続できます。社員のメール・パスワード登録は不要です。

スマホの相談・業務入力はPC停止中も利用でき、共有MDはサーバーからGoogle Driveへ保存します。PCファイル作業は起動後に実行します。端末別の連携解除、同期履歴・プロジェクト管理に対応します。

導入・認証・再連携条件は [さくら同期サーバーの説明](server/sakura/README.md) を参照してください。以下の旧リモート方式は互換設定として残っています。

# エスコAIアシスタント (esco-ai)

エスココーポレーション社員向けの社内AIデスクトップアプリ。
Claude Agent SDK × Electron。CLIを知らない社員でもチャット画面だけで
「相談・文章作成」「提案資料の作成」「フォルダ整理・リネーム」ができる。

## 起動（開発）

```
cd C:\Users\k-ima\Desktop\esco-ai
npm start
```

スモークテスト（GUIなしでSDK疎通確認）: `npm run smoke`

## 配布用ビルド（ポータブルexe）

```
npm run dist
```

→ `dist/` に単一exeができる。社員のPCにはこれを配るだけ（インストール不要）。

## 仕組み

- `main.js` — Electronメイン。ウィンドウ・IPC・設定・作業フォルダ管理
- `agent.js` — Claude Agent SDKの実行（AI-Buddyの実証済みパターンを簡略化して流用）
- `settings.js` — 設定(userData/settings.json)と月間利用額(usage.json)の保存
- `renderer/` — チャットUI（バニラJS、ビルド不要）

## モードとモデル

| モード | 内容 | 既定モデル |
|---|---|---|
| チャット | 相談・文章下書き | claude-sonnet-5 |
| 資料作成 | 提案書等を作業フォルダにファイル保存 | claude-sonnet-5 |
| フォルダ整理 | 📁で選んだフォルダ内の分類・リネーム（削除はせず`_不要`へ移動） | claude-sonnet-5 |

モデルはモードごとに⚙設定で変更可（Haiku 4.5 / Sonnet 5 / Opus 5）。

## スマホ連携（リモートチャット）

⚙設定 →「スマホ連携」で方式を選ぶと、スマホのブラウザからチャットモードだけ使える。

| 方式 | 仕組み | QR読み取り | PC停止中 |
|---|---|---|---|
| かんたん接続 | `remote.js` + `tunnel.js`（Cloudflare Quick Tunnel、cloudflared.exe同梱） | アプリ起動のたび | 使えない |
| サーバー経由 | `relayClient.js` が中継サーバー（`server/`、VPS上）へ外向きWS常駐 | 初回1回だけ | サーバーのクラウド頭脳がチャットのみ代打（readonly固定）＋会話はPC復帰時に引き継ぎ |

共通の制限: リモートは常に チャットモード / 作業フォルダなし / ノーマル許可 に固定。書き込み系の操作は承認カードがスマホに出る。スマホUIは `renderer/mobile/`（両方式共通）。

- サーバー経由の設定: 管理者が `server/scripts/make-connect-code.mjs` で生成した「接続コード」を設定画面に貼るだけ。サーバー構築は `server/README.md`
- 検証: `npm run remote-sim` / `npm run relay-sim`（いずれもAPIコスト0） / `electron . --remotetest`（トンネル疎通） / `electron . --relaytest`（中継サーバー疎通、`ESCO_TEST_RELAY_URL`・`ESCO_TEST_ORG_KEY` で接続先指定）

## 並行作業と資料の渡し方

### セッション履歴（デスクトップ版）

- 会話は左の「セッション履歴」に自動保存。最初のメッセージがタイトルになり、タイトル検索ができます。
- 「＋ 新しい会話」で過去の履歴を残して次の会話を開始。履歴を選ぶと会話本文・最後に送信した作業モード・作業フォルダを復元し、続けて話せます。
- 保存先は Electron の `userData/sessions/`。アプリのインストール先と別なので、終了・再起動・更新後も保存ファイルが残ります。このPCのユーザー単位の履歴です。
- 実行中は会話切替を制限。同じ会話を別ウィンドウで開いている場合は、そのウィンドウを表示します。
- SDKのセッションIDも保存して続きを再開。SDK側の履歴が消えた場合や作業フォルダを変えた場合は、直近40メッセージ・最大60,000文字を文脈として引き継ぎます。ツールの内部ログや過去の許可状態は復元しません。
- この機能を導入する前の会話、スマホ側の会話は、この一覧への自動取り込み対象には含みません。
- 保存テスト: `node test/sessions-test.cjs`。画面・IPCテスト: `electron test/history-ui.cjs`（一時フォルダとテスト応答を使い、API課金なし）。後者の `TEST_DATA` を渡し `electron test/history-ui.cjs --restore <TEST_DATA>` でプロセス再起動後の復元も検証できます。

- **🪟 新しいウィンドウ** — ウィンドウごとに独立した会話・作業フォルダ・AI実行。
  複数案件を同時に進められる（Claude Codeの複数ターミナルと同じ感覚）
- **ドラッグ&ドロップ** — PDF等を画面に落とすと「（ファイル: パス）」が入力欄に入る。
  「この資料を要約して提案書にして」のような依頼ができる（作業フォルダ外のファイルも読み取りは可）

## 安全設計

- 読み取り系ツールのみ自動許可。書き込みは作業フォルダ内のみ自動許可
- Bashは危険パターン（shutdown/format/全消し等）を拒否。
  「ファイル操作を自動許可」チェック（フォルダ整理モードで既定ON）がOFFなら都度確認バー表示
- WebSearch/WebFetchは無効化（社内利用の想定外動作を防ぐ）
- `settingSources: []` でこのPCのCLAUDE.md等は読み込まない

## APIキー

- ⚙設定から入力 → userDataのsettings.jsonに保存（PCごと）
- 未設定の場合、このPCではClaude Codeのログイン認証にフォールバックする
  （＝開発中は光強代表の課金。**北山社長のConsoleでキーを発行したら必ず設定すること**）
- 費用表示は `result.total_cost_usd` の積算（概算・レート150円/$固定表示）

## 未実装・次のステップ

- [ ] 北山社長のAnthropic Consoleアカウント作成 → APIキー差し替え（電話待ち）
- [ ] 社員ごとのキー/ワークスペース分け（Consoleで人別に発行して個別集計）
- [ ] `npm run dist` でexe化して配布テスト
- [ ] 実利用フィードバックでプロンプト調整

## v0.2.0 ワークスペースと業務共有

[更新内容と初回設定](RELEASE_NOTES_0.2.0.md) を参照してください。
- 左側のプロジェクトから会話を作成し、中央で並行作業、右側でファイルを確認します。
- セッションの「…」から名前変更・移動・ピン留め・アーカイブ・ゴミ箱・復元ができます。
- 「私の業務を教える」に入力すると、設定済みの共有先へスタッフ別の業務MDを保存します。
- v0.2.0では、以前の「実行中は会話切替を制限」という制約を廃止しました。切替時も承認待ちの質問を保持します。
- `npm run dist` 相当の配布ビルドは `electron-builder --win --x64 --publish never` で作成できます。
